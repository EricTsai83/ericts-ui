# Implementation Plans

Generated by the improve skill on 2026-07-03 against commit `f46af2b`. Execute in
the order below unless dependencies say otherwise. Each executor: read the plan
fully before starting, honor its STOP conditions, and update your row when done.

> Selection note: this audit ran non-interactively, so per the skill's default
> the top 5 findings by leverage were planned. Deferred findings below are
> vetted and real — they were not selected this run, not rejected.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Verification baseline — vitest, CI, dependency hygiene | P1 | M | — | DONE (2026-07-03, reviewed & approved, merged to main at `aa3e3f1`) |
| 002  | Fix search scoring tier order + table-driven tests     | P1 | S | 001 | DONE (2026-07-03, reviewed & approved, merged to main at `286be21`) |
| 003  | Harden four shipped registry components                | P1 | M | 001 | DONE (2026-07-03, reviewed & approved, merged to main at `d1e4ece`) |
| 004  | Make the registry pipeline self-verifying              | P2 | M | 001 | DONE (2026-07-03, reviewed & approved, merged to main at `a07ee5a`) |
| 005  | Split the preview god-module, lazy-load demos          | P2 | L | 001 | DONE (2026-07-03, 2 revision rounds — lint hygiene + dead `usePreviewCornerSlot` export, reviewed & approved, merged to main at `eae4e5a`) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 002, 003, 004, 005 all require 001: it installs vitest (002, 003 write tests), creates the CI workflow (004 appends a step to it), and provides the green-baseline safety net for 005's large refactor.
- 004 and 005 touch opposite sides of one contract: `scripts/validate-registry-display.mjs` AST-parses the `previews` object literal in `components/registry-preview.tsx`. Either order works, but whichever lands second must re-run `pnpm display:check` and honor the other's shape (both plans carry STOP conditions for this).
- 002 must not land before 001 because 001's tests are told NOT to pin the buggy tier ordering that 002 fixes.

## Deferred findings (vetted, not planned this run)

Worth planning in a future run, roughly in leverage order:

- **DEBT-02 — measurement duplication**: the ResizeObserver + border-box measurement logic is hand-inlined in `smooth-height` (both variants), `expandable-toolbar`, and `adaptive-drawer` although `use-element-height` exists and `multi-step` already consumes it via `registryDependencies`. Consolidate after 003 lands.
- **DEBT-03 — reduced-motion strategy split**: shipped `ui/` components all use `useReducedMotion` from `motion/react`; the registry's own `use-reduced-motion` hook has zero internal consumers; CSS-only files use media queries. Pick one JS convention and dogfood the shipped hook, or document why not.
- **SECURITY-02 — no response-hardening headers**: `next.config.ts` has no `headers()`; add a baseline CSP (report-only first), `X-Content-Type-Options`, `Referrer-Policy`, and frame-ancestors for `/view/*`.
- **SECURITY-03 — search HTML sink fragility**: `components/docs-search.tsx` `renderContent` renders strings via `dangerouslySetInnerHTML`; safe today only because upstream escapes. Escape the docs branch locally so the sink stops depending on an upstream guarantee.
- **TESTS-04 remainder — loud required-file reads**: `lib/registry-code.tsx`'s `readOptionalFile` swallows read errors for *required* registry files into empty code blocks; make required reads throw at build (keep the css-only existence probe quiet). Plan 004 adds the validator-side check; this is the belt to its suspenders.
- **DOCS-01 — no props/API reference on item pages**: detail pages show source + install but no prop tables; consider a structured `meta.api` block in `registry.json` or generation from exported types.
- **DOCS-02 — blocks docs promise an empty surface**: `content/docs/blocks.mdx` and the nav document `/blocks`, but zero `registry:block` items exist; soften to "coming soon" or ship DIRECTION-01.
- **DOCS-03 — no llms.txt**: the `/docs/*.md` rewrite already exists for LLM consumption; generate `public/llms.txt` from `registry.json` at build time.
- **DEPS-02 — scoped major upgrades**: `lucide-react` 0.x→1.x (icon renames, every icon import affected), TypeScript 6, ESLint 10. Each as its own small plan with a build+lint gate; routine minor/patch bumps need no plan.

## Direction options (maintainer's call, evidence-grounded — from the audit)

- **Ship the first block** (strongest grounding): routes, eight block display categories in `lib/registry-display.ts:88-135`, docs, and the `/registry → /blocks` redirect all exist with zero `registry:block` items; primitives for an auth/onboarding block (`otp-input`, `multi-step`, `status-button`, `feedback-popover`) are already shipped.
- **Faceted filtering on browse pages**: `lib/registry.ts:122` `getRegistryItemFacets` is built and has zero callers; items already carry categories/tags/effects/cssOnly.
- **Complete the CSS-only variant track**: infra (`getCssOnlyFiles`, code tabs, badges) is generalized but only 2 items have variants; aligns with "performance first".
- **Grow hooks into the reserved categories**: `lib/registry-display.ts` reserves interaction/state/browser hook categories; extractable candidates live inside `context-cursor.tsx` (pointer logic) and `expandable-tabs.tsx` (roving focus).
- **MCP server over the registry**: search + structured metadata already exist server-side; an agent-facing search/install surface is mostly wiring.

## Findings considered and rejected

- **context-cursor's persistent global `<style>` node** (CORRECTNESS-06): id-guarded singleton, rules only apply to elements carrying a cleaned-up data attribute; ref-counting removal adds complexity for no user-visible benefit. Accepted as-is.
- **`.env.example`** (DX-03): every env var (`NEXT_PUBLIC_BASE_URL`, `VERCEL_*`) is optional with a hardcoded fallback in `lib/site-url.ts`; nothing to document at onboarding.
- **Build-time double read of primary registry files** (PERF-02): amortized SSG work, negligible; fix opportunistically if `lib/registry-code.tsx` is touched for the loud-reads follow-up.
- **Shared easing-token module** (DEBT-04): the easing values differ per component family and a shared runtime import conflicts with the copy-paste registry model; at most a documentation convention. Not worth a plan.
- **fumadocs-mdx at 15.x vs fumadocs-core 16.x**: intentional — 15.x declares `fumadocs-core ^16` as its peer; not version drift.
